import React, { Component } from 'react';

export default class LoginComponent extends Component {
    state = {
        email: '',
        password: '',
    }

    render() {
        return (
            <div></div>
        )
    }
}