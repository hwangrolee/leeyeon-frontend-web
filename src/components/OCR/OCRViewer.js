import React, { Component } from 'react';
import styles from './OCRViewer.scss';
import classNames from 'classnames';

const cx = classNames.bind(styles);

class OCRViewer extends Component {

    render () {
        return (
            <div>

            </div>
        )
    }
}

export default OCRViewer;